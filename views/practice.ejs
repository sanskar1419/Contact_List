<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Hey Sanskar this side</h1>
    <!-- use of for loop inside ul -->
    <ul>
      <% for(let i = 1 ; i<10 ; i++) { %>
      <li><%= i %></li>
      <% } %>
    </ul>
    <!-- use of if else -->
    <% if(false) { %>
    <h1>Hii it is good over here</h1>
    <% } else { %>
    <p>Its not good</p>
    <% } %>
  </body>
</html>
